# 既知のバグ
現在自分自身で把握しているバグを、不具合報告をする想定で記載しています。

# (不具合１) 正解発表後の正解変更時にユーザーがリロードするとスコアが早く反映される

## 1. 概要
問題の正解発表後に、正解を変更を行った後。
ユーザーがリロードすると、正解発表前に正解変更に伴うスコア変動がそのまま反映される

## 2. 発生環境
* **OS:** Windows 11
* **ブラウザ:** Google Chrome (最新版)

## 3. 再現手順
1. 解答者側 の 画面を開く
2. 管理者側 で 問題番号0のクイズ（もしくは正解設定が2番のクイズ）を出題する
3. 解答者側 で 制限時間内に2番を解答する。
4. 管理者側 で 正解発表までそのまま行う。（解答者側 で 正解時スコアが反映されていることを確認）
5. 管理者側 で 正解変更機能を使い。 正解を2番から1番に変更して確定。 
6. 解答者側 で リロードを行う
7. 解答者側 で 正解時のスコアが０になっていることを確認。 

## 4. 実際の結果
* 正解発表される前に、正解変更によるスコア変動が反映される。

## 5. 期待される結果
* 正解変更によるスコア変動が正解発表後に反映

## 6. 重要度
* [ ] 重大
* [ ] 高
* [×] 中 （通常進行で、ほぼ発生しないイレギュラーな状況うえゲームの処理や公平性に影響しないため）
* [ ] 低

## 7. 補足
* 通常通りの進行でアンサーチェック中などにリロードをしても同様の現象は起きません。
　（正解発表時に 正解設定をベースにスコアデータを作成 という処理のため）

* 正解発表と同時に "その問題のスコアデータ" が作成されるため
　その問題のスコアデータがある状態の場合。
　正解変更した瞬間に、スコア部分の変更や再計算が行われています。
　通常は正解発表と同時にスコア反映が行われるのですが、リロード時にもスコアが反映されます。

  通常時
  A.アンサーチェック→B.正解発表→C.スコア計算・反映
  例えば3問目の場合。
  アンサーチェック時にリロードしても、1～2問目の合計スコアのみの再反映なので問題はありません。
  
  不具合時
  A.正解発表→B.スコア計算・反映→C.正解変更→D.スコア再計算（内部的に）→E.「該当箇所」→F.正解発表→G.再計算されたスコア反映
  例えば3問目の場合。
  A.正解発表の時点で1～3問目の合計スコアが生成
  D.スコア再計算も1～3問目の合計スコアでの再計算が行われます。
  ユーザーに反映されるのはF.正解発表のときのみです。
  E.「該当箇所」でユーザーがリロードを行うと
　リロードで再反映されるデータは D.スコア再計算も1～3問目の合計スコアでの再計算 でしたものになります。
  
  出題者側が正解設定ミスしたまま正解発表＋ユーザーが局所的なタイミングでリロード という発生条件
  ユーザーが 一足早く自分の正解不正解の把握 ができてしまうだけでゲーム的な影響は軽微
  なので不具合としての優先順位は低いとは思われます。
